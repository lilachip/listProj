import { FilterSearchedListItemPipe } from './filter-searched-list-item.pipe';

describe('FilterSearchedListItemPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSearchedListItemPipe();
    expect(pipe).toBeTruthy();
  });
});
